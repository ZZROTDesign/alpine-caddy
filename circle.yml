machine:
  services:
    - docker

dependencies:
  pre:
    - sudo pip install docker-compose

test:
  pre:
    - docker-compose build
  override:
    - docker-compose up -d


deployment:
  dockerhub:
    branch: master
    commands:
      - $TRIGGER_DOCKER_HUB_BUILD
