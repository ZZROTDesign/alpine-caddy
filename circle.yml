machine:
  services:
    - docker

test:
  override:
    - docker-compose build
    - docker-compose up -d


deployment:
  dockerhub:
    branch: master
    commands:
      - $TRIGGER_DOCKER_HUB_BUILD
