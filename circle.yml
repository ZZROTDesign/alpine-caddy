machine:
  services:
    - docker

test:
  override:
    - docker build -t alpine-caddy .
    - docker run alpine-caddy


deployment:
  dockerhub:
    branch: master
    commands:
      - $TRIGGER_DOCKER_HUB_BUILD
